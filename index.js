// sum(1, 1) = 1;
// sum(1, 2) = 2 + 1;
// sum(1, 3) = 3 + 2 + 1;

function sum(fromN, toN) {
  if (fromN === toN) {
    return toN;
  }

  // return
  // Sum all the values from fromN up to toN
}

console.log(sum(3, 7));
module.exports = sum;
